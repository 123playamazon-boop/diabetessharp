/** Preço comercial exibido no portal (cobrança real: Stripe / equipa — demo só regista assinatura no admin). */
export const AMAZON_LEADS_PRO_MONTHLY_USD = 49.99;

/** Cap de linhas por edição diária (segunda a sexta). */
export const AMAZON_LEADS_DAILY_PUBLISH_CAP = 50;
